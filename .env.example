# Cache OpenSky responses for N seconds to protect rate limits.
STATES_CACHE_SECONDS=10

# Threading for parallel state-vector parsing (defaults scale with CPU cores).
# PARSE_WORKERS=8
# PARSE_CHUNK_SIZE=500
